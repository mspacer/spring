db:
  username: test
  password: passtest

spring:
  jpa:
    properties.hibernate:
      hbm2ddl.auto: update

logging:
  level:
    root: warn
    com.msp.spring: debug
    org.springframework.jdbc: trace
  file:
    name: spring-test.log
    path: /
  pattern:
    file: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} : %msg%n"